#include <stdio.h>

// do not compile

int main(int argc, char **argv)
{
   //  while (i < 100)
   //  sum += data[i], ++i; // here the value of data is added into sum and then i is incremented

    int i = (5, 10); // here 10 is assigned to i
    printf("%d\n", i);

    return 0;   
}
